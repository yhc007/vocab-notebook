# 도메인 없이 IP+HTTP로 노출(인증서 없음). 외부 80 포트로 받아 앱(8080)에 프록시.
:80 {
    reverse_proxy 127.0.0.1:8080
}

# 도메인이 생기면 위 :80 블록 대신 아래를 사용 → Caddy가 Let's Encrypt로 HTTPS 자동 발급:
# vocab.example.com {
#     reverse_proxy 127.0.0.1:8080
# }
